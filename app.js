// Copy the entire React component code from the artifact here
// (The full PortersFiveForcesAnalyzer component)

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<PortersFiveForcesAnalyzer />);
